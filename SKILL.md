---
name: siyu-analysis
description: Generate evidence-based Chinese private-domain (私域) operations reports from standardized Excel workbooks and publish them as HTML or Lark/Feishu documents with model-specific charts. Use for private-domain cohort comparisons, first-order customer activation, membership-card analysis, and the workflow “upload Excel → collect report context → analyze → create a structured report”.
---

# 私域运营效果分析

只使用用户提供的工作簿和业务信息。区分观察、推断与建议；观察性分组不得写成确定性因果结论。

## 1. 输入流程

用户上传 `.xlsx` 后，必须先收集报告信息，再开始分析。

必填：

- 品牌
- 数据时间
- 追踪窗口
- 观测周期

选填，允许留空：

- 品类
- 人均消费
- 淡旺季
- 会员权益卡价格与权益

选填项为空时写“未提供”，不得阻塞分析，不得用示例值补齐。用户填写“请帮我分析”时，只能基于工作簿或有来源的资料分析；无法验证时保留为待补充信息。

## 2. 工作簿校验

标准工作表为 `模型一`、`模型二`、`模型三`、`模型四`，数据从第 3 行开始：

| 工作表 | 去除首列后的字段 |
| --- | --- |
| 模型一 | 数据窗口、客户分层、客户数、周期内总客户数、分布占比、平均消费次数 |
| 模型二至四 | 数据窗口、客户类型、客户分层、客户数、周期内总客户数、分布占比、平均消费次数 |

要求：

- 使用工作簿中的实际窗口、分组和频次层级，禁止使用脚本默认值或示例值。
- 校验每个窗口、分组、样本量、频次层级与占比；异常时说明缺项，不得猜测映射。
- 若缺少模型四，继续完成模型一至三，并在执行摘要和模型四章节写“不可评估”。不得因此删除其他必需章节。
- 模型二、三的追踪期可以没有 `0次消费客户` 行；其他缺失需明确说明。
- 高频用户统一定义为工作簿的 `4次及以上消费客户`。
- 观测期有几个月，就必须纳入全部月份；不得因脚本固定窗口漏掉最后一期。

## 3. 分析规则

1. **模型一｜效果验证**：描述同一私域存量人群基准期与全部追踪期的平均频次、0 次占比和 4 次及以上占比。保留季节性、活动与筛选偏差。
2. **模型二｜对比验证**：只在相同追踪期比较私域组和非私域对照组。包含 0→1 新客的初始基准不得作为前后效果。
3. **模型三｜归因验证**：比较私域首单新客和非私域首单新客；`2次及以上复购率`为 2 次、3 次、4 次及以上占比之和。两组仍可能存在渠道、优惠、门店与自选择差异，不得写“可直接归因”。
4. **模型四｜放大验证**：比较持卡与非持卡人群。购卡属于自选择；没有实验或匹配设计时只写相关性。缺少价格、权益、核销成本和毛利时不得计算 ROI。

每次组间比较必须同时报告：样本量、窗口、绝对值、相对差或百分点差、解释边界。

## 4. 模型章节内部顺序

每个可用模型严格按以下顺序：

1. 模型说明
2. 筛选口径：样本人群、时间锚点、剔除规则
3. 对应标准图表（模型可用时固定 2 张）
4. 数据明细表
5. 核心发现
6. 口径注意

飞书文档中的图表必须位于对应模型章节内，不得统一堆在文末。若使用 `docs +media-insert` 先追加图片，随后必须用 block move 将图片移到该模型的图表锚点，并删除临时锚点。

### 标准图表清单

不得用临时自拟图表替代下列标准图表，也不得只满足图表数量而改变指标口径：

| 模型 | 文件名 | 图表标题 |
| --- | --- | --- |
| 模型一 | `chart_01_dist.png` | 图1：拉新客户消费频次分布变化 |
| 模型一 | `chart_02_avg.png` | 图2：拉新客户平均消费次数变化趋势 |
| 模型二 | `chart_03_compare.png` | 图3：私域组 vs 非私域组消费频次分布对比（追踪后1月） |
| 模型二 | `chart_04_trend.png` | 图4：私域组 vs 非私域组平均消费次数趋势对比 |
| 模型三 | `chart_05_compare.png` | 图5：私域首单新客 vs 非私域首单新客复购频次对比（追踪后1月） |
| 模型三 | `chart_06_repurchase.png` | 图6：私域首单新客 vs 非私域首单新客2次及以上复购率对比 |
| 模型四 | `chart_07_cross.png` | 图7：四类客户平均消费次数对比（追踪后1月） |
| 模型四 | `chart_08_s_trend.png` | 图8：购卡 vs 未购卡客户平均消费趋势 |

要求：

- 模型可用时必须生成该模型对应的 2 张图，标题、指标、分组与文件名均按清单执行。
- 图3、图5、图7使用工作簿中的第一个追踪月；图2、图4、图6、图8纳入全部追踪月份。
- “S卡”只是旧模板中的示例名称。报告统一使用用户提供的权益卡名称；未提供名称时写“购卡 vs 未购卡”，不得擅自写“S卡”。
- 模型缺失时不伪造图表，也不生成空白占位图；保留该模型“不可评估”章节，并从期望图表数中扣除 2 张。
- 图表生成优先调用 `scripts/generate_charts.py`，不得以临时绘图脚本改写标准指标。

## 5. 不可省略的报告结构

按以下顺序输出，任何交付模式均不得省略：

1. 报告标题与元数据
2. 执行摘要
3. 模型一至模型四
4. 交叉对比与归因
5. 数据攻防推演
6. 运营建议
7. 最终结论
8. 附录
9. 原始 Excel 附件（飞书模式）

### 交叉对比与归因

综合多个模型的方向、强弱和替代解释，形成递进证据链。明确哪一层是前后描述、同期对照、首单新客对比或权益卡相关性；不得把一致方向自动升级为因果。

### 数据攻防推演

至少覆盖：

- 私域组是否因高频用户更愿意加入而领先？
- 跨期变化是否受淡旺季、节假日或活动影响？
- 样本量和观测周期是否足以支撑结论？
- 持卡用户是否本来就更高频？模型四缺失时说明无法回答。

每条按“质疑 → 数据回应 → 尚未排除的解释”书写。

## 6. 飞书排版与命名

文档标题严格使用：

```text
[品牌]私域运营效果分析报告（[数据时间]，[追踪窗口]，[观测周期]）v.[YYYYMMDD]
```

- 方括号仅表示变量，实际标题不得保留方括号。
- 版本日期使用报告生成当天的北京时间，格式为 8 位数字；例如 `v.20260727`。
- 数据时间保留用户提供的原始表达，不擅自缩写或改写。

飞书报告沿用参考文档的视觉语言，但不得为了排版改变数据含义：

- 标题后放报告说明与四列元数据分栏：数据时间、追踪窗口、观测周期、分析维度。
- 业务背景与全局口径使用浅黄提示块：背景 `rgb(254,255,240)`，边框 `rgb(255,186,107)`。
- 筛选口径使用浅蓝提示块：背景 `rgb(240,244,255)`，边框 `rgb(255,186,107)`。
- 核心发现使用浅绿提示块：背景 `rgb(240,251,239)`；口径注意使用浅黄提示块。
- 数据风险或重要异常使用浅红提示块：背景 `rgb(254,241,241)`；跨模型关联和最终结论使用浅灰提示块：背景 `rgb(245,246,247)`。
- 模型验证类型使用橙色文字 `rgb(222,120,2)`；正向、负向数字分别使用绿色 `rgb(46,161,33)`、红色 `rgb(216,57,49)`。颜色只表达方向，不代替文字判断。
- 执行摘要优先使用“结论维度 / 判断 / 依据”表格，并用三列指标卡展示最多 3 个最关键指标；数据不足时减少指标卡，不得凑数。
- 模型章节使用三级标题，严格保持“说明 → 筛选口径 → 图表 → 数据表 → 核心发现 → 口径注意”。
- “数据攻防推演”每个质疑使用引用块；运营建议使用浅蓝提示块，并标明优先级、依据模型、执行时限和可验证指标。没有成本或实验依据时，不写虚构的量化预期。
- 图表宽度统一约 800 至 900 像素，带清晰标题、图例、数据标签和图注；继续遵守图表位于对应模型章节内的规则。
- 高亮块用于口径、发现、风险和行动，不把普通叙述全部装进高亮块。

## 7. 交付前硬门禁

生成后必须运行：

```bash
python scripts/validate_report_structure.py <report.html-or-xml> \
  --available-models 1,2,3,4 \
  --expected-charts 8 \
  --expected-title "<按第6节模板生成的完整标题>"
```

模型四缺失示例：

```bash
python scripts/validate_report_structure.py <report.html-or-xml> \
  --available-models 1,2,3 \
  --expected-charts 6 \
  --expected-title "<按第6节模板生成的完整标题>"
```

门禁失败时禁止创建或交付飞书文档。修复后重新运行。

此外必须：

- 抽查至少 5 个关键数字与工作簿一致。
- 检查全部图表标签、窗口顺序、分组顺序和数值。
- 检查图表文件名和标题与“标准图表清单”逐一对应；只满足数量但名称或指标不符视为门禁失败。
- 检查每张图位于对应模型标题之后、下一模型标题之前。
- 检查没有示例品牌、固定月份、固定卡价、默认样本或生成器水印。
- 检查“交叉对比与归因”“数据攻防推演”“附录”均存在。
- 检查缺失模型仍保留“不可评估”章节。
- 检查文档标题符合命名模板，包含品牌、数据时间、追踪窗口、观测周期和当天版本日期。
- 检查各类提示块的颜色语义一致，正负向颜色不与文字结论冲突。

## 8. 交付

优先创建飞书文档；无法使用飞书时生成 HTML。提供飞书链接或报告路径，并说明缺失模型和选填背景信息。原始 Excel 放在飞书文档末尾作为附件。
